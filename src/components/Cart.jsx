import React from 'react';

const Cart = () => {
    const cartData = localStorage.getItem("shopping-cart")
    console.log(cartData)
    return (
        <div>
            
        </div>
    );
};

export default Cart;